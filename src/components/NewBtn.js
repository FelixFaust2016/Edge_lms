import React from "react";

const New = (props) => {
  return (
    <div style={{ marginTop: "50px" }}>
      <a href="#" style={{ fontSize: "0.75rem", fontWeight: "600" }}>
        {props.link}
      </a>
    </div>
  );
};

export default New;
